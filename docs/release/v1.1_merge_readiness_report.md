# v1.1 Merge Readiness Report

## Summary

- Branch: `feature/v1.1-chat-native-flow`
- HEAD: `e0ebccc3992134f2e59e6fa473557e7bb2ac31c0`
- VERSION: `1.0.1`
- Working tree before report creation: clean
- Recommendation: ready for merge after reviewer acceptance of known sandbox-dependent gaps.

## Key Commits

| Commit | Description |
|---|---|
| `7d598e5` | `feat: add v1.1 chat-native backend flow` |
| `3cdd9d2` | `test: cover v1.1 chat-native backend flow` |
| `f9ee627` | `docs: document v1.1 chat-native implementation state` |
| `c824f47` | `fix: assign reply tasks to command author` |
| `a91fc69` | `feat: persist snooze from task callbacks` |
| `2911757` | `feat: apply template deadline rules to scheduled tasks` |
| `d41e2fb` | `feat: add scheduled task run ledger` |
| `e0ebccc` | `feat: add group assignment WebApp UI` |

## Checks

| Check | Result |
|---|---|
| Backend pytest | passed, `440 passed` |
| Backend ruff | passed |
| Alembic heads | passed, `e1f2a3b4c5d6 (head)` |
| Alembic history | command completed |
| WebApp build | passed, `npm run build` |
| WebApp typecheck | covered by `npm run build` via `tsc --noEmit` |
| WebApp lint | not configured in `webapp/package.json` |
| VERSION | unchanged, `1.0.1` |

WebApp build still emits the known Vite chunk-size warning caused by the Ant Design bundle. This is not a build failure and remains a future optimization item.

## Routes And APIs Verified

Frontend routes:

- `/group-assignments`
- `/group-assignments/:taskId`

Backend endpoints:

- `POST /api/tasks/group-assignment`
- `GET /api/tasks/{task_id}/group-report`
- `POST /api/task-templates`
- `GET /api/task-templates`
- `GET /api/task-templates/{id}`
- `PATCH /api/task-templates/{id}`
- `DELETE /api/task-templates/{id}`
- `POST /api/scheduled-tasks`
- `GET /api/scheduled-tasks`
- `GET /api/scheduled-tasks/{id}`
- `PATCH /api/scheduled-tasks/{id}`
- `DELETE /api/scheduled-tasks/{id}`

## Diff Scope

`git diff main...HEAD --name-only` includes v1.1 backend flow, tests, migrations, docs, and WebApp group assignment UI. Major areas:

- MAX normalized event contract and mock fixtures.
- Rule-based deadline parser.
- Reply-to task creation and self-task fallback.
- Callback payload parsing and callback service.
- Reminder snooze and notification delivery.
- Daily manager summary.
- Group assignment backend model/API/report.
- Task templates and scheduled tasks.
- Scheduled task run ledger/idempotency.
- Group assignment WebApp pages and navigation.

## Secret Scan

Command: `git diff main...HEAD` was scanned with the standard project credential-safety pattern for SSH material, MAX/Bitrix24 credentials, database credentials, generic auth query fragments, and external hook path fragments.

Result: no real secrets found.

The scan matched only synthetic redaction/payload test strings and redaction regex patterns:

- `backend/app/modules/notifications/service.py`
- `backend/tests/test_notification_delivery.py`
- `backend/tests/test_bot_callbacks.py`

These are sanitized test cases used to verify that unsafe callback payloads and delivery errors are rejected or redacted.

## Known Gaps

- Real MAX sandbox payloads are still pending because the bot is not yet through moderation.
- Real MAX callback event shape must be confirmed before enabling production callback mapping.
- Real MAX direct-message behavior and DM-unavailable errors must be verified before relying on personal reminders.
- WebApp open button behavior and URL/context passing must be verified in real MAX.
- Message formatting and rate-limit behavior remain sandbox-dependent.
- Selected-member audience UI/API is not fully exposed in the WebApp; current Group Assignment UI creates assignments for active chat members according to backend MVP behavior.
- Bitrix24 real pilot is intentionally postponed and not part of v1.1 Chat-Native Task Flow.
- WebApp list for group assignments currently uses `/api/tasks` and filters `task_type=group_assignment` on the frontend until a dedicated backend filter/list endpoint is added.

## Recommendation

Ready for merge from a local automated-check perspective:

- backend tests pass;
- ruff passes;
- Alembic has a single head;
- WebApp build passes;
- VERSION remains `1.0.1`;
- no real secrets were found;
- no real MAX API calls were performed.

Merge/deploy should still be treated as a feature-branch rollout with sandbox validation follow-up for real MAX payloads, callbacks, DMs, WebApp buttons, formatting, and rate limits.
