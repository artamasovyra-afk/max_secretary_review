# Backup Restore Check — max_secretary v1.0.0

Документ фиксирует результат проверки backup/restore процедуры после пилотного деплоя `v1.0.0`.

## Scope

- Release: `v1.0.0`
- VERSION: `1.0.0`
- VPS project path: `/opt/max_secretary/app`
- Compose file: `docker-compose.prod.yml`
- Production database: `max_secretary`
- Restore target: separate temporary test database

Production restore was not performed. Production data was not overwritten.

## Backup File

- Backup file: `backups/max_secretary_20260519_190707.sql.gz`
- Backup size: `25K`
- Backup size bytes: `24948`
- gzip integrity check: passed

## Restore-test Method

The backup was restored into a temporary database inside the existing PostgreSQL container:

- test database: `restore_test_20260519_191149`
- restore command: `gunzip -c <backup_file> | psql -d <test_db>`
- cleanup: temporary database dropped after verification

This validates dump readability without touching the production database.

## Restored Tables

Required tables were present after restore:

- `organizations`
- `users`
- `chats`
- `tasks`
- `task_assignees`
- `bitrix_user_mappings`
- `bitrix_task_links`

Restored public table count: `18`.

Sample restored row counts:

- `bitrix_task_links`: `3`
- `bitrix_user_mappings`: `4`
- `chats`: `25`
- `organizations`: `29`
- `task_assignees`: `44`
- `tasks`: `29`
- `users`: `82`

## Production Safety Check

Production database control values were checked before and after restore-test:

- before: `18` public tables, `29` tasks
- after: `18` public tables, `29` tasks

Result: production database was not affected.

## Result

Backup/restore procedure check: passed.

Recommended follow-up:

- Test full restore on a separate staging VPS before major upgrades.
- Configure scheduled backups and external secure storage.
- Periodically repeat restore-test for fresh backups.
