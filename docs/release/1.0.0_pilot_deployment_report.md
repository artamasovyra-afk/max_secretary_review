# Pilot Deployment Report — max_secretary v1.0.0

## 1. Release information

- VERSION: `1.0.0`
- tag: `v1.0.0`
- commit: `d06af7be3e262a877c469869c5a62daf3da397ac`
- release commit: `7b71778 chore: prepare pilot release 1.0.0`
- smoke fix commit: `d06af7b test: align release smoke with production auth`

## 2. VPS deployment state

- deployment path: `/opt/max_secretary/app`
- compose file: `docker-compose.prod.yml`
- deployed ref: `v1.0.0`
- git status: working tree clean

The VPS was explicitly switched from `main` to the fixed pilot release tag `v1.0.0`.

## 3. Containers

- `backend`: Up / healthy
- `postgres`: Up / healthy
- `redis`: Up / healthy
- `worker`: Up
- `webapp`: Up
- `nginx`: Up

`nginx` is the public entry point and publishes port `80`.
`postgres` and `redis` are available only inside the Docker network and are not published externally.

## 4. Checks performed

- docker compose config: passed
- alembic upgrade head: passed
- `/api/health`: OK
- WebApp routes:
  - `/`: HTTP 200
  - `/tasks`: HTTP 200
  - `/dashboard`: HTTP 200
- release smoke: OK
- backend tests before release: 250 passed
- ruff: passed
- webapp build: passed
- scripts syntax: passed

Healthcheck response:

```json
{"status":"ok","service":"max_secretary"}
```

## 5. Backup

- Backup status: completed after deployment
- Backup file: `/opt/max_secretary/app/backups/max_secretary_20260519_190707.sql.gz`
- Backup size: `25K` (`24948` bytes)
- gzip integrity check: passed

The backup file is stored under `backups/`, which is excluded from git.

## 6. Security notes

- Production `.env` is not committed.
- `DEV_AUTH_ENABLED=false` in production is an expected secure state.
- Protected Bitrix24 endpoints without auth return `401`.
- Real Bitrix24 webhook URL is not configured.
- Real MAX sender credentials are not enabled.
- PostgreSQL and Redis are not exposed externally.

## 7. Known limitations

- `1.0.0` is a pilot-stable version, not full production certification.
- Full MAX WebApp auth is not implemented yet.
- Bitrix24 sync is manual.
- Two-way Bitrix24 sync is not implemented.
- Real file storage is not implemented.
- Closed contour bundle requires separate infrastructure acceptance.
- Observability stack is not complete.

## 8. Recommended next steps

- Verify backup restore on a test environment.
- Configure regular backups.
- Run browser acceptance testing for WebApp.
- Test real MAX webhook on a separate staging environment.
- Test real Bitrix24 webhook on a separate staging environment.
- Prepare pilot user guide.
- Prepare `1.0.1` plan for bugfixes and hardening.
