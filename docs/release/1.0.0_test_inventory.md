# Test Inventory — max_secretary v1.0.0

Дата снимка: `2026-05-20`.

## 1. Total Tests

Backend pytest collection:

```text
253 tests collected
```

Последняя локальная проверка после worker hardening:

```text
253 passed
ruff check . passed
```

WebApp release check:

```text
npm run build passed
```

## 2. Covered Areas

### Health

Covered by:

- `backend/tests/test_health.py`
- `backend/tests/test_app_import.py`
- `backend/tests/test_module_status_routes.py`

Includes app import, `/api/health`, health response shape and module status endpoints.

### Tasks

Covered by:

- `backend/tests/test_task_enums.py`
- `backend/tests/test_task_service.py`
- `backend/tests/test_tasks_api.py`

Includes task creation, filters, detail view, updates, cancellation, assignees, observers, comments, files metadata, responses, acceptance/rejection and inbox summary.

### Users / Chats / Organizations

Covered by:

- `backend/tests/test_users_api.py`
- `backend/tests/test_chats_api.py`
- `backend/tests/test_organizations_api.py`
- `backend/tests/test_models_import.py`

Includes CRUD flows, chat members and model imports.

### Reminders

Covered by:

- `backend/tests/test_reminders_service.py`
- `backend/tests/test_reminder_jobs.py`
- `backend/tests/test_reminder_rules_api.py`
- `backend/tests/test_reminder_scheduler.py`
- `backend/tests/test_worker_jobs.py`

Includes reminder selection logic, reminder rules API, overdue marking, scheduler behavior, enabled/disabled mode and worker lifecycle logs.

### MAX Adapter

Covered by:

- `backend/tests/test_bot_command_parser.py`
- `backend/tests/test_bot_command_service.py`
- `backend/tests/test_bot_event_normalization.py`
- `backend/tests/test_bot_max_webhook.py`
- `backend/tests/test_max_api_client.py`
- `backend/tests/test_max_sender.py`

Includes normalized webhook events, command parsing, bot service behavior, webhook secret handling, MAX client adapter and switchable sender behavior.

### WebApp Build

Covered by:

- `npm run build`
- `scripts/smoke_test_webapp.sh`

Includes TypeScript compile, Vite production build and nginx route smoke checks for `/`, `/dashboard`, `/tasks`, `/api/health` and `/openapi.json`.

### Bitrix24 Client / Mapper / Sync

Covered by:

- `backend/tests/test_bitrix24_api_client.py`
- `backend/tests/test_bitrix24_task_mapper.py`
- `backend/tests/test_bitrix24_sync_service.py`
- `backend/tests/test_bitrix24_sync_api.py`
- `backend/tests/test_bitrix_user_mappings_api.py`
- `backend/tests/test_integration_enums.py`
- `backend/tests/test_integration_models.py`

Includes REST client adapter with mocked HTTP transport, task mapper, manual sync service, sync status endpoint, retry endpoint, user mapping CRUD and integration models.

### RBAC / Auth Preparation

Covered by:

- `backend/tests/test_auth_dependency.py`
- `backend/tests/test_auth_policy.py`
- Bitrix24 sync API tests using auth headers

Includes `AuthContext`, dev header auth behavior, role policy decisions and RBAC protection for Bitrix24 endpoints.

## 3. Smoke Scripts

Available smoke scripts:

- `scripts/smoke_test_mvp.sh`
- `scripts/smoke_test_webapp.sh`
- `scripts/smoke_test_bot_webhook.sh`
- `scripts/smoke_test_reminders.sh`
- `scripts/smoke_test_bitrix24_connector.sh`
- `scripts/smoke_test_bitrix24_mapping.sh`
- `scripts/smoke_test_max_sender.sh`
- `scripts/release/smoke_release_1_0.sh`

Release smoke wrapper:

- `scripts/release/smoke_release_1_0.sh`

It runs the core MVP, WebApp, Bitrix24 disabled-mode and reminders smoke checks. Bitrix24 protected checks can be skipped in production when `DEV_AUTH_ENABLED=false`, which is expected secure behavior.

## 4. Areas That Need Stronger Tests

### WebApp UI behavior

- Add browser-level tests for Dashboard, Tasks and Task Details.
- Verify Ant Design forms and modals beyond build-level checks.
- Check browser console for critical errors in CI or staging.

### Auth and RBAC integration

- Expand endpoint-level RBAC coverage beyond Bitrix24 endpoints.
- Add tests for future MAX WebApp auth payload validation.
- Add negative tests for unauthorized task actions once full auth is connected.

### Worker observability

- Worker heartbeat and Docker healthcheck are implemented on current `main` after the `v1.0.0` tag.
- Keep test coverage for heartbeat freshness and enabled/disabled worker lifecycle.
- Verify worker shutdown behavior in Docker/VPS smoke.

### Backup / restore automation

- Add a safe automated restore-test script for temporary database restore checks.
- Include table-count and required-table assertions.
- Ensure production restore remains manual and requires explicit confirmation.

### Offline bundle

- Add CI/VPS validation for `docker-compose.offline.yml`.
- Add checksum verification smoke for generated offline manifests.
- Test `docker load` flow in a disposable environment.

### Deployment and release gate

- Add a route snapshot check to detect accidental API route removal.
- Add smoke script output normalization and machine-readable summary.
- Add explicit release gate docs for local vs CI vs VPS checks.

### Integration edge cases

- Add Bitrix24 sync tests for partial mappings and default responsible/created-by values.
- Add MAX sender tests for additional API error shapes once real MAX API behavior is validated.
- Add retry/backoff behavior tests that avoid real sleeps through dependency injection.

### Data model migrations

- Add migration upgrade/downgrade checks in CI or a dedicated PostgreSQL test job.
- Validate Alembic heads/current against a real PostgreSQL service in CI.
